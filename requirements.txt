setuptools
requests
wheel
numpy>=2.0.0
faster-whisper
sounddevice

# AI Processing dependencies
PyYAML>=6.0
python-dotenv>=1.0.0

# TTS shared dependencies (both Chatterbox and Qwen3-TTS need these)
torch>=2.0.0
torchaudio>=2.0.0
soundfile>=0.12.0
pydub>=0.25.0  # For MP3 and other audio format support
librosa>=0.11.0
safetensors>=0.5.3
transformers>=4.46.0

# Chatterbox-specific dependencies (install chatterbox itself separately)
# pip install chatterbox-tts --no-deps
s3tokenizer
diffusers>=0.29.0
omegaconf>=2.3.0
resemble-perth>=1.0.1
conformer>=0.3.2

# Qwen3-TTS: pip install qwen-tts (installs its own deps)
# flash-attn is required for Qwen3-TTS. Install AFTER torch:
#   pip install flash-attn --no-build-isolation

# Optional: for auto-type feature
pyautogui
pyperclip

# NiceGUI for modern UI
nicegui>=1.4.0
pywebview>=4.0.0  # Required for NiceGUI native mode
PyQt6>=6.0.0  # GUI backend for pywebview native mode
PyQt6-WebEngine>=6.0.0  # WebEngine for pywebview
QtPy>=2.0.0  # Qt abstraction layer for pywebview
